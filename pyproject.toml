[project]
name = "push-fight-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.1",
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
    "pygame>=2.5.0",
    "gymnasium>=0.29.0",
    "pyxel>=2.6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-cov>=4.1.0",
]
